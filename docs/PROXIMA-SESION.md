# Prompt de arranque para la próxima sesión de ONE

> Copiar y pegar tal cual al abrir Claude Code en `C:\Users\torre\OneDrive\Desktop\ONE`. Actualizado el 17-sep-2026 a las 10:35 (v0.2a.2.1 PUBLICADA: visual al 50-60 % real de la tarjeta, sw `one-v17`; antes v0.2a.2 PUBLICADA: protagonismo visual; plan de escalada en docs; antes v0.2b4.1 PUBLICADA: proveedores gratis Groq/NVIDIA/Cerebras, reparto por cuota, paralelismo, visual sin bloquear; antes v0.2b4 PUBLICADA: tanda recordada, indicador, chip que juega, Gemini vivo en visuales; siguiente v0.2c 8 áreas renombradas; antes: v0.2b3 PUBLICADA y desplegada: átomo amplio y dinámico, Conectar desde la app instalada, Gemini gratis en la cascada; plan v0.2a.2 escrito y siguiente; spec §8 aprobada).

---

Hola. Continuamos ONE (app personal de preguntas sin teclado). Lee primero `README.md`, `docs/PROXIMA-SESION.md` (este fichero), la spec `docs/superpowers/specs/2026-09-14-one-v0.2-generacion-y-repaso-design.md` (v0.2: repaso, servidor, átomo) y la v0.1c §8 y la memoria del proyecto. No reabras decisiones cerradas.

**Dónde está todo**
- Repo público `carlostorresadvisory/one`, rama `main`, publicado en https://carlostorresadvisory.github.io/one/ (GitHub Pages; la PWA instalada recibe los cambios en la segunda apertura; caché del service worker `one-v10`).
- Verificación: `node --test tests/*.test.js` (137) y `npx playwright test` (33 tests a 375×812, 393×852 con zonas seguras del iPhone simuladas y 430×932; `assertTarjetaSinScroll` mide también cada hijo de la tarjeta y que nada sobresale de la caja del visual; capturas `docs/capturas/v0.1c-*.png`, `v0.1d-*.png`, `v0.1e-*.png`). `node --test tests/*.test.js` → 198.
- Banco: `datos/banco.json`, 295 preguntas generadas y verificadas con el criterio de utilidad de `tools/criterio.js` (renovación del 12-sep: 482 verificadas → fuera 58 duplicadas y 158 de utilidad 1; puntuaciones en `datos/utilidad.json`, rechazadas con motivo en `datos/rechazadas.json`). Imágenes: `datos/imagenes.json` (105, Wikimedia Commons, dominio público o CC con atribución) generadas por `tools/buscar-imagenes.js`. Pipeline solo `:free` salvo `--permitir-pago --tope-eur N`; Carlos aprobó 0,50 $/día el 12-sep (gasto real total < 0,15 $).
- Proceso de la tanda v0.1c: plan `docs/superpowers/plans/2026-09-12-one-v0.1c-mazo-vertical.md`, ejecutado con subagent-driven-development (libro de a bordo con todas las decisiones en `.superpowers/sdd/2026-09-12-one-v0.1c-mazo-vertical/progress.md`, git-ignored, léelo si sigue existiendo).

**Qué se hizo el 12/13-sep (v0.1c "mazo vertical")**: la partida y el repaso son un mazo vertical de tarjetas a pantalla completa (deslizar arriba/abajo, pasar y volver, tarjeta de cierre con "Volver a ellas / Terminar igual"); confianza Baja/Media/Alta dentro de la tarjeta tras el enunciado, opcional, editable incluso tras responder (`cambiarConfianza` en el motor recalcula XP, consolidación y prioridad); respuesta fija; explicación siempre dentro de la tarjeta, sin scroll y sin cambiar tamaños de letra (se pliegan primero la imagen, luego las respuestas, luego se recorta la explicación con toque para alternar); botones "Preguntar a" ChatGPT / Claude / Gemini (modo IA de Google) con el prompt en la URL; repaso vertical (cifras → una tarjeta por fallada o frágil → Otra partida / Inicio); imagen de Commons con atribución en la tarjeta respondida; banco renovado por utilidad; regla de concordancia añadida al criterio.

**v0.2a PUBLICADA (14-sep, 15:45; main = `63133c8`, sw `one-v10`)** — "Repaso siempre accesible": botón Repaso en el HUB → mazo vertical sin fin con TODAS las tarjetas respondidas (imagen o visual), orden por prioridad de repaso (`ordenarRepaso` en `motor.js`: pendientes → frágiles → les toca → resto), chips de filtro por área (máscara de degradado a la derecha, `sessionStorage` `one.repasoFiltro`), cierre "Otra vuelta / Volver"; `registrarRespuesta` guarda `ultimaRespuesta`/`ultimaCorrecta` (las anteriores muestran solo la correcta); el mazo vertical vive ahora en `mazo.js` (extracción textual verificada, app.js 2.623 → 2.092 líneas). Spec `docs/superpowers/specs/2026-09-14-one-v0.2-generacion-y-repaso-design.md` (§2), plan `docs/superpowers/plans/2026-09-14-one-v0.2a-repaso.md`, libro de a bordo `.superpowers/sdd/2026-09-14-one-v0.2a-repaso/progress.md` (git-ignored). Tests: 223 unitarios, 39 e2e. Lección repetida: la revisión final con Opus volvió a encontrar el único Critical (el mazo se montaba con la vista `hidden`, medidas a 0, sin cascada: 6/30 tarjetas reales desbordaban; también latente en el resumen) → fix de orden `mostrarVista` antes de montar + e2e sin resize. Adversarial gratis: 10 hallazgos, 3 refutados, resto asumidos (`adversarial-v02a-triage.md`). **Pendiente de Carlos**: probar el Repaso en el iPhone (cerrar y abrir dos veces).

**v0.2a.2.1 PUBLICADA (17-sep, 10:35; main = fusión de `v0.2a2.1-visual-50`; sw `one-v17`; solo Pages, el servidor no cambia)** — decidido el 17-sep 01:51 tras probar v0.2a.2 en el iPhone: visual (imagen, gráfico o tarjeta tipográfica) con SUELO 50 % y TECHO 60 % de `.tarjeta` (antes 40-55 vh), imagen a `cover` con el pie DENTRO de la propia visual; sin botón "Siguiente" (mismo `data-test="siguiente"`, ahora es la flecha ↓ de 36 px; se pasa de pregunta deslizando o tocándola); confianza junto a "Preguntar a" con chips pequeños; tarjeta tipográfica (`construirVisualClave`, HTML no SVG) con layout propio por tipo — lista NUMERADA completa para `ordenar` (lo que Carlos llamó "muy mala" el 17-sep), fila corregida para el dato erróneo, respuesta grande + descartadas para test de 4, veredicto + frase para V/F; explicación y enunciado recortados se tocan y abren a pantalla completa (excepción deliberada a "sin toques"); gráficos de datos (`barras`/`comparacion`/`linea-tiempo`) repartidos en el hueco nuevo de la visual. Spec `docs/superpowers/specs/2026-09-17-one-v0.2a2.1-visual-50-design.md`, plan `docs/superpowers/plans/2026-09-17-one-v0.2a2.1-visual-50.md`, ledger `.superpowers/sdd/2026-09-17-one-v0.2a2.1-visual-50/` (git-ignored, `task-N-brief.md`/`task-N-report.md` por tarea, rulings R0-R15 en `progress.md`). Detalle de preferencias en la memoria `one-preferencias-producto.md`.
- **Hecho (Tareas 1-7, commits `e2f4d32`…`12f6726` + Tarea 7 con este commit)**: T1 modelo puro de la tarjeta tipográfica; T2 tarjeta tipográfica HTML por tipo; T3 zona de acción (confianza + chips + flecha, sin "Siguiente"); T4 visual al 50-60 % real con cover y pie dentro; T5 explicación/enunciado recortados tocables a pantalla completa; T6 gráficos de datos repartidos en el hueco (con ronda de corrección del desborde real que el barrido de 44 ids encontró); T7 (esta) barrido del banco REAL completo (295/295) en el repaso con las tres garantías juntas — 100 % con visual, ninguna por debajo del 50 %, ninguna desborda —, capturas nuevas `docs/capturas/v0.2a2.1-{duchamp,ordenar,grafico,texto-completo}-375.png` (miradas con Read y comparadas contra las maquetas antes de commitear) y esta actualización de docs. Verificación tras la Tarea 7: **629 unitarios, 127/127 e2e** (`npx playwright test --workers=2`, sin ningún filtro, sin flake esta vez — incluida "confianza compacta a 32px").
- **Hallazgo y arreglo de la Tarea 7 (defecto evidente y pequeño de una tarea anterior, no una decisión de diseño nueva)**: la lista numerada de `ordenar` (T2, `.visual-clave-lista`) se veía COMPLETA pero SIN NUMERAR — justo lo que el propio comentario del código decía que arreglaba ("muy mala" → lista numerada). Dos causas independientes, comprobadas en vivo por separado: (1) `display:flex;flex-direction:column` en el `<ol>` — un `<li>` que pasa a ser flex item deja de generar caja de `::marker` en todos los navegadores basados en Blink y en Firefox, aunque `getComputedStyle` siga leyendo `list-style-type:decimal`; (2) quitado eso, `list-style-position:outside` (el valor por defecto) seguía sin pintar porque `overflow:hidden` en `.visual-clave-item` (imprescindible para el recorte con "…" de una línea) se comía el marcador por estar fuera de la caja del `<li>`. Arreglado con `list-style-position:inside` (el número entra en el flujo del texto y comparte su propio recorte con "…", nunca se pierde) + flujo de bloque normal en vez de flex para el espaciado entre ítems. Confirmado con capturas a 4× zoom antes/después; no toca ninguna otra tarea.
- **Cierre hecho el 17-sep (08:50-10:35)**: Tarea 8 ejecutada (`node --env-file=.env tools/visualizar.js --solo-pendientes --limite 40 --aplicar`, 0 €): 15 de 33 candidatas resueltas (10 visuales nuevos + 13 explicaciones acortadas, commit `0cb6423`), 18 sin respuesta por cuota gratis agotada (HTTP 429 en gemini-3.6-flash, groq gpt-oss-120b y gemma free) y 5 rechazadas por esquema → **quedan 22 preguntas sin imagen ni gráfico: repetir la tanda otro día** (ojo: los scripts de tools/ leen solo process.env, hay que lanzarlos con `--env-file=.env`). Revisión final Opus EJECUTANDO la app a 390×844 (el iPhone de Carlos) y 375×667: encontró 2 Critical + 3 Important que ninguna revisión de diff vio (línea "Tu respuesta" oculta con clave; viñetas de `comparacion` huérfanas a 18 px en 14/33; clave `error` sin tachar el dato falso; apaisado inutilizable; e2e sin exigir visibilidad) → ola final (`99ed356`…`0fdd88f`) + I4 (`a95f2e7`), re-verificados ejecutando: Duchamp 52,1 % a 390×844 y 56,9 % a 375×667; barrido 295/295 en [50,00 %, 58,43 %] sin desbordar; 4 formatos con altura idéntica. Adversarial nemotron gratis (2 llamadas, 0 €): 6 "Critical" refutados con el código, 3 menores aplicados. 630 unitarios, 128 e2e. Capturas para Carlos en `docs/capturas/v0.2a2.1-*.png` (375) y en el scratchpad de la sesión (`final-*-390.png`).
- **Pulido pendiente para v0.2c (revisión final, Minor)**: docblock de `ajustarEncaje` sin los pasos (g)/(h); las 6 `ordenar` sin imagen no muestran la respuesta del jugador al fallar; `role="img"` en la clave oculta la semántica del `<ol>`; aire vertical en la clave `ordenar` (lista centrada, podría escalar la letra); en apaisado el dedo no desplaza (`.mazo{touch-action:none}`), solo rueda/teclado; título del gráfico de barras truncado (preexistente).
- **Visto por Carlos en el iPhone tras publicar (17-sep 18:08)**: (1) la tarjeta parece más pequeña porque el indicador de tanda "0 de 10 · ~2 min" (v0.2b4) la empuja ~175 px hacia abajo mientras hay una tanda pendiente — la tarjeta mide igual; para v0.2c: indicador como línea fina o dentro de la cabecera. (2) Pidió generar "reestructuración" (Economía) y le salieron preguntas de otras áreas: el servidor lleva desde las 06:11 sin generar (`/salud`: 8 en cola, última OK 04:11Z) por cuota gratis agotada (429 en Gemini/Groq/gemma; la tanda de visuales de la mañana usó las mismas claves), y "Jugar" rellena en silencio con preguntas del banco de cualquier área → v0.2c: avisar cuando la tanda no llega y no rellenar sin decirlo; reservar cuota para el servidor (no lanzar visualizar.js de día). (3) "Sin imágenes": Guernica es test sin foto en Commons → tarjeta tipográfica por diseño; las 22 sin nada esperan la tanda gratis.
- **Arreglos urgentes publicados el 17-sep a las 18:40 (main `2b1505d`, sw `one-v18`, VPS redesplegado)**: (1) el indicador de tanda vive DENTRO de la cabecera en el sitio de racha/nivel (`.cabecera--con-tanda`, `visibility:hidden` sobre `.cabecera-estado`), compacto (30 px), sin reservar hueco en `<main>`: la tarjeta recupera ~90 px; (2) cascadas urgentes `generador`/`verificador` (tools/openrouter.js) y `VERIFICADOR_VISUAL` (tools/visualizar.js) con último recurso `nex-agi/nex-n2.5-pro:free` (+ nemotron en el visual); `TIMEOUT_LLAMADA_URGENTE_MS` 30→60 s y `MS_MAX_REPOSICION` 45→90 s. Revisión Sonnet: publicable; 630 unitarios, 128 e2e. **Límite real medido tras desplegar**: con la cuota gratis del día agotada (Gemini/Groq/gemma 429-503), una tanda urgente real de Economía llevaba 9 min en "generando, 0 de 10" (nemotron 50 s/llamada, nex devuelve JSON truncado, los trabajos de fondo compiten por NVIDIA con 120 s de timeout). Se recupera solo al renovarse las cuotas (Gemini ~09:00 hora española). **Para que un día así no vuelva a dejar ONE sin tandas, decisiones de Carlos**: activar el nivel gratis de Cerebras (hoy 402) o permitir el escalón ultrabarato (DeepSeek V4 flash por OpenRouter, ~0,003-0,005 $ por tanda) con tope diario; y no lanzar `visualizar.js` de día con las mismas claves. Deuda técnica: los trabajos de fondo deberían pausarse mientras haya una tanda urgente en vuelo; subir `maxTokens` del verificador para nex.
- **Escalón ultrabarato ACTIVADO (Carlos, 17-sep 18:50: "permito escalón ultrabarato, tope diario 10 céntimos")**: en el VPS `/opt/one/.env` → `PERMITIR_PAGO=1`, `TOPE_EUR_DIA=0.10` (el servidor no arranca con pago sin tope). Cascadas urgentes (servidor/generacion.js#intercalarPagoBarato, main `a307b73`): gratis rápidos → pago barato (GLM-4.7-flash generar, DeepSeek V4 flash verificar, gemini-2.5-flash-lite para el visual) → gratis lentos (NVIDIA, :free). Medido tras desplegar: tanda urgente de Historia 10/10 en 88 s, 0,0024 €; gasto del día 0,0135 €. Con el tope, ~40 tandas/día en el peor caso. El gasto se ve en `/salud` (`gastoHoyEur`) y por llamada en `/opt/one-datos/llamadas.log`. Ojo: POST /generar por curl necesita `"urgente":true` (la app lo manda); sin él, la tanda va por las cascadas de fondo y bloquea la cola.
- **Dos decisiones pendientes de Carlos** (medidas, no tocadas — "no arreglar" explícito del brief de la Tarea 7):
  1. El techo del 60 % NO se alcanza a 375×667 con la explicación real de varias preguntas del banco (medido: 52-56 % con texto corto; Ruling R9). El presupuesto fijo de la cascada no deja más sitio libre en ese viewport — no es un bug, es el invariante "sin hueco muerto" cumpliéndose sin llegar al techo. Alternativa sobre la mesa: juntar "esta pregunta está mal" en la misma fila que Confianza (ganaría ~4,5 puntos de alto, ~26 px, para la visual) — cambiaría el layout de esa fila.
  2. El paso (g) de la cascada de encaje (`tarjeta--feedback-minimo`, mazo.js, Ruling R10) oculta CUATRO cosas para ganar el alto que necesita, no solo el aviso de nivel: `.titulo-tarjeta` y `.instruccion-error` ("Toca la fila que está mal", la cabecera propia del tipo "error") y `.cambio-nivel`/`.cambio-nivel-area` ("Nivel N ↑"). Esto NO es hipotético: la propia captura `v0.2a2.1-duchamp-375.png` (art-091, tipo "error", 375×667) lo activa — comparada con `maqueta-B2-B3.png`, faltan "Por qué importan estas obras" y "Toca la fila que está mal" que la maqueta sí muestra (corregido en el informe de la Tarea 7, que en la primera pasada decía "coincide con la maqueta" sin matizarlo). La captura de `eco-089` SÍ muestra "Nivel 2 ↑" porque ese caso no necesita llegar al paso (g). **Nota de contexto para la revisión final**: el iPhone real de Carlos es 390×844 (la captura original de Duchamp que mandó es 1170×2532, 3x) — con más alto que los 375×667 de esta captura de prueba, es probable que el paso (g) no llegue a activarse ahí; queda por comprobar en la revisión final con ese viewport exacto. Falta decidir si perder ese bloque en el caso extremo (375×667 con tipo "error") es aceptable o si alguno de los cuatro debería tener prioridad sobre otro paso de la cascada.
- **Deudas menores anotadas en el ledger** (sin tocar en la Tarea 7, no bloquean): sin test de regresión para la cascada CSS `.visual-completa-clave` vs `.visual-completa-media`; el `touch-action` de la caja de texto no se recalcula en un resize con la superposición abierta; `ordenar` filtra ítems inválidos en silencio; `pieSolapa` es una comprobación tautológica; docblock cortado en `construirTarjetaRespondida` (~l.3040, cosmético). Después de publicar: v0.2c 8 áreas.

**v0.2a.2 PUBLICADA (16-sep, ~23:00; main = fusión de `v0.2a2-protagonismo-visual`; sw `one-v16`; solo Pages, el servidor no cambia).** "Protagonismo visual" (spec §8 con la enmienda del 16-sep: **40 % suelo, 55 vh techo, borde superior fijo**, decidida por Carlos tras ver maquetas con imagen a toda la tarjeta y texto encima, descartadas; maquetas en el scratchpad de la sesión). Qué trae: (T1) `visuales.js` `textoVisualClave`/`construirVisualClave`: tarjeta tipográfica SVG 320×180 (clave de la respuesta en grande, primera frase del enunciado, área) que garantiza visual al 100 % sin red, incluidas las `srv-`; nunca lanza. (T2) `app.js` `construirTarjetaRespondida` con orden fijo cabecera de UNA línea (área · nivel · marca de repaso · contador) → `.zona-imagen` (imagen → visual de datos → clave; `error` de img cae a la capa siguiente) → enunciado → respuesta → explicación → confianza → Preguntar a; CSS `min-height: max(160px, 40%)`, `max-height: 55vh`; cascada revelada en `mazo.js`: compacta-1 → enunciado 14 px → clamp explicación (mín. 2) → zona reducida (`max(120px, 30%)`) → clamp enunciado → clamp explicación 1; la clave llena la zona (degradado en la zona, rect del SVG oculto ahí). (T3) `.visual-completa` (`index.html`): toque en la visual → pantalla completa con clon de img/svg, pie con atribución completa, botón ← 44 px, trampa de foco de 2 paradas, `inert` en `<main>`, cierres por toque / deslizar ≥ 60 px (`touch-action: none` + `pointercancel`) / Escape / botón; fundido 160 ms anulado con reduced-motion. Verificación: 627 unitarios, 101 e2e; Sonnet por tarea con revisor y rondas; **Opus final EJECUTANDO la app con el banco real** (7.ª vez que encuentra el Critical: el deslizamiento táctil nunca cerraba por `touch-action` heredado) + adversarial nemotron gratis (20 hallazgos, 0 altos); barrido 295/295 con visual (167 imágenes · 96 gráficos · 32 claves), ratio mínimo 40 %, 52 tarjetas medidas en 3 resoluciones sin desborde. Ledger `.superpowers/sdd/2026-09-14-one-v0.2a2-protagonismo-visual/` (git-ignored; rulings R1-R15). **Carlos debe**: abrir la app DOS veces en el iPhone; probar una partida (visual arriba al responder, toque → pantalla completa, deslizar abajo cierra: esto es lo único que no se pudo probar en Safari real), el resumen y el repaso. **Deudas**: secundario de la clave en 2 líneas; try/catch duplicado en `construirVisualClave`; clave 16:9 pequeña a pantalla completa; "cramped padding" en `.confianza` (hook impeccable, preexistente); acotar longitud del enunciado en el pipeline (v0.2c); flake e2e "v0.2b4.1 §5 (extra)"; apaisado no soportado. **Idea aceptada en principio (v0.2a.3)**: marca "imagen protagonista" por imagen (foto/cuadro sin texto, calculada en el pipeline de imágenes) → esas tarjetas con imagen a toda la tarjeta y cuadro semiopaco oscuro con el texto (2-3 de cada 10). **Plan de escalada** `docs/2026-09-16-plan-escalada.md` (adversarial hecha): gratis para el público (decisión de Carlos), banco compartido en CDN, Supabase UE, worker en hosting gestionado, ultrabarato (DeepSeek V4 flash / GPT-5 nano por OpenRouter) solo de respaldo; pendientes de Carlos: tope diario del escalón ultrabarato, PWA o tienda, plazo de beta; boss descartado. **Siguiente aprobado**: v0.2c "8 áreas renombradas" (partición aprobada el 15-sep) → A generación por consumo → B ficha rápida/anillo diario/push → beta cerrada.

**v0.2b4.1 PUBLICADA (16-sep, 18:05; main = fusión de `v0.2b4.1-gratis-rapido`, 49 commits; sw `one-v15`; VPS redesplegado).** "Gratis y rápido", tras fallar la tanda real de producción de v0.2b4 (278 s, 0/5, cuotas gratis agotadas) y la decisión de Carlos de no pagar. Spec `docs/superpowers/specs/2026-09-15-one-v0.2b4.1-gratis-rapido-design.md`, plan `docs/superpowers/plans/2026-09-15-one-v0.2b4.1-gratis-rapido.md`. Qué trae: proveedores `groq:*`, `nvidia:*`, `cerebras:*` en `tools/openrouter.js` (tabla `PROVEEDORES`, claves `GROQ_API_KEY`/`NVIDIA_API_KEY`/`CEREBRAS_API_KEY` en `ONE/.env` y `/opt/one/.env`, salto inmediato en 429/503/402, `timeoutMs` 30 s en urgentes, `signal` abortable); `tools/cuota.js` (reparto por cabeceras `x-ratelimit-*`, estimación de tokens con salida ≤ 1.200); `servidor/generacion.js` (lotes en paralelo, visual con plazo 20 s → `visualPendiente`, reposición de rechazadas ≤ 45 s, corte de fondo al llegar urgente); `servidor/cola.js` (`hechas` por pregunta, medias móviles separadas urgente/fondo, `completarVisualesPendientes` ≤ 3 intentos); `POST /estado {desde}` devuelve `actualizadas` y `ahora`; cliente `aplicarActualizaciones` (`one.actualizadoHasta`), `esVisualValido`, `trabajoAtomoTerminal` corregido (`parcial` ya no cierra la tanda). Medición local (15-sep noche, cuotas gastadas): 68 s/9 entregadas, 103 s/7, 109 s/9 con 3 fondos delante. Objetivo < 60 s NO alcanzado por cuotas gratis (3.6-flash 429 diario, gemma-4:free 429, Cerebras 402 hasta activar el plan gratuito). Rulings en el ledger `.superpowers/sdd/2026-09-15-one-v0.2b4.1-gratis-rapido/progress.md`. **Carlos debe**: abrir la app DOS veces en el iPhone; activar el plan gratuito de Cerebras (cloud.cerebras.ai › facturación). **Siguiente (v0.2b4.2, palancas gratis)**: medir con cuota fresca por la mañana; abortar el visual de fondo al llegar un urgente; reposición por tanda; deudas del ledger (#12 esperas reales en tests, #16 cascadas de fondo lentas de noche, #19 `msDeCabecera('7.5s')`).

**v0.2b4 PUBLICADA (15-sep, 18:00; main = fusión de `v0.2b4-generacion-clara`, 28 commits; sw `one-v14`; VPS redesplegado).** "Generación que se entiende", tras el feedback de Carlos en el iPhone (09:52). Spec `docs/superpowers/specs/2026-09-15-one-v0.2b4-generacion-clara-design.md`, plan `docs/superpowers/plans/2026-09-15-one-v0.2b4-generacion-clara.md`. Qué trae: `tanda.js` (la tanda en curso se guarda en `localStorage` `one.atomoTrabajo` y se retoma sola al abrir; un corte de red ya no la borra: 5 sondeos fallidos → "Sin conexión, se retomará al abrir", y recuperar el foco la retoma); indicador fijo `data-test="indicador-tanda"` en segunda fila bajo la cabecera, visible en todas las vistas ("5 de 10 · ~40 s" → "Tanda lista · 10", tocarlo la juega; en partida/repaso el toque no navega para no perder la tirada); sustituye al chip `tanda-lista`; chip "N preguntas nuevas" arranca una partida con ellas (máx. 10, texto "14 nuevas · Jugar 10"); "+" en las áreas; "Regenerar temas" en el átomo (mismo comportamiento de siempre: sustituye el anillo). Servidor: Gemini gratis al frente de las cascadas de `tools/visualizar.js` — estaba MUERTO por HTTP 400 (`tools/openrouter.js` mandaba el campo `reasoning` de OpenRouter al endpoint de Google; arreglado: `CAMPOS_PROPIOS_OPENROUTER` se quitan del body cuando el destino es Gemini); explicación de 25-40 palabras desde la primera llamada (`criterio.js` y prompts alineados) con `saltarAcortado`; media móvil de segundos por pregunta (solo tiempo activo por lote), `segundosPorPregunta` en `/generar` y `/trabajo/:id`, retención 1 h de terminados. Verificación: 494 unitarios, 74 e2e; Sonnet por tarea; Opus final EJECUTANDO app+servidor (encontró los 2 Critical: el 400 de Gemini y el corte de red que borraba la tanda); adversarial nemotron gratis (25 objeciones, triaje en el ledger); ola única de corrección + re-revisión. Coste 0 €. **Tanda real de 5 en local**: 491 s antes del arreglo → 148 s después, con 429 (cuota gratis agotada) en los verificadores; el objetivo de < 40 s queda por medir con cuota fresca. **Carlos debe**: abrir la app DOS veces en el iPhone (la primera sigue en v0.2b3 por la caché); probar Generar → cerrar la app a mitad → reabrir → "Tanda lista" → tocar; el chip de nuevas; "Regenerar temas". **Deudas**: `MAX_PARTIDA_NUEVAS` duplica `N_PARTIDA`; `posicion`/`calcularPosicion` sin consumidor; tope `TERMINADOS_MAX` sin test; el anillo se queda en 0 la primera mitad (hechas avanza de 5 en 5); apaisado no soportado; "Generar" bloqueado en la misma sesión sin red hasta recuperar el foco; `visualizar.js:333` "2 frases" ya alineado. **Siguiente aprobado**: v0.2c "8 áreas renombradas" (partición aprobada el 15-sep 10:13, ver memoria `one-estado-v0.md`), luego v0.2a.2 protagonismo visual (plan escrito; su caché será `one-v15`).

**v0.2b3 PUBLICADA (15-sep, 01:25; main `73df5e3` fusión + `5ae6c9c` ids Gemini; Pages sirve `one-v13`; VPS desplegado, `/salud` ok).** Qué trae: (T1) `tools/openrouter.js` ids `gemini:*` → endpoint OpenAI-compatible de Google con `GEMINI_API_KEY_GRATIS`, gratis, coste 0, reintento único 429/503; cascadas `generador` = `gemini:gemini-flash-lite-latest`, `gemini:gemini-3.6-flash`, nemotron…; `verificador` = `gemini:gemini-3.6-flash`, gemma…; `esModeloGratis` exportado y compartido por `servidor/generacion.js` e `index.js` (antes filtraban solo `:free` y Gemini nunca llegaba). (T2) `POST /subtemas {area, ruta, excluir?}`: anillo 1 = primeros 6 hilos, con `excluir` pagina el resto sin modelo y luego pide al modelo con exclusión; filtro normalizado (sin acentos/mayúsculas) de la respuesta; clave de caché `[area, ruta, excluir.length]` (riesgo asumido: un solo jugador); límites 160 caracteres por elemento de ruta/exclusión, 30 elementos; hilos nuevos en Economía (Finanzas corporativas y M&A; Reestructuraciones e insolvencia; Inversión y mercados; Banca y regulación) y Ciencia (Física moderna). (T3) átomo dinámico según Carlos ("no es que dé vueltas, sino que pinchas y salen más cosas; al clicar tiene que pasar algo"): sin órbita giratoria, texto dentro de nodos (radio 38, guion en palabras largas), al tocar cambian núcleo y ruta al instante y aparecen 6 nodos de espera no pulsables, fila "Jugar el área / Repasar" mientras carga o genera, aviso de lentitud a 20 s, Atrás cancela, tope 6 anillos con aviso, nodo "Más…" (excluir acotado a 30/160; "No hay más por ahora" / "No se pudo cargar más"); timeout de `/subtemas` 90 s. (T4) hoja "Conectar" (tocar el punto de estado del HUB o del átomo, pegar el enlace o `servidor token`) porque la app instalada en iOS tiene almacenamiento separado de Safari; foco, aria-live, aria-label con estado. Verificación: 455 tests, 64 e2e, revisor Sonnet por tarea, Opus final EJECUTANDO app+servidor (encontró el Critical del límite de 80 caracteres que rompía 26 de 50 hilos, el timeout de 10 s, el aviso pegado y el área táctil sobre Comenzar; todo arreglado y re-revisado), adversarial nemotron 0 $ (30 objeciones, triaje en `.superpowers/sdd/2026-09-14-one-v0.2b3-atomo-amplio-gemini/adversarial-triage.md`). **Tanda real tras publicar**: Gemini 2.5 devolvía 404 (Google lo retiró para cuentas nuevas) → cambiados a los alias medidos desde el VPS (`gemini-flash-lite-latest` 0,6 s, `gemini-3.6-flash` 1,2 s); `/subtemas` anillo 2 en 1,0 s con `gemini:gemini-flash-lite-latest`, coste 0. **Tanda real de 5 en Economía › Finanzas corporativas › LBO (01:15): lista 5/5 en 100 s, 0 €; 24 llamadas, 7 a Gemini (4 generaciones con flash-lite-latest, 2 verificaciones con 3.6-flash) y el resto a los `:free` de OpenRouter para el paso de visual (`tools/visualizar.js` tiene cascadas propias sin Gemini: candidata a deuda para acelerarlo).** **Carlos debe probar en el iPhone**: (1) en Safari abrir el enlace de conexión de siempre; (2) en la app instalada, tocar el punto junto a Comenzar → pegar el enlace → Conectado; (3) mantener pulsada Economía → átomo: tocar "Más…" (finanzas), tocar "Finanzas corporativas y M&A" → anillo 2 en ~1 s → Generar. **Deudas anotadas**: calidad de los subtemas de `gemini-flash-lite-latest` en anillos ≥ 2 (mezcla ramas poco relacionadas, p. ej. "Cadenas de valor" bajo Reestructuraciones: afinar prompt o subir a 3.6-flash para subtemas); `/salud` sigue diciendo `version: 0.2b1` (constante sin subir); reintento 429/503 y `usage.cost` ausente en modelos de pago (solo relevante si se activa pago); hueco muerto bajo el átomo; ruta recortada ":conomía" con la hoja Conectar abierta; `corto` muy agresivo; test intermitente de concurrencia en `tests/servidor-cola.test.js`.

**v0.2b2 PUBLICADA (14-sep, ~21:00; sw `one-v12`) — cliente conectado + Átomo.** Rama `v0.2b2-cliente-atomo` fusionada en main. Plan `docs/superpowers/plans/2026-09-14-one-v0.2b2-cliente-atomo.md`, ledger `.superpowers/sdd/2026-09-14-one-v0.2b2-cliente-atomo/progress.md` (rulings, triage adversarial, deudas). `sincronizacion.js` (configuración saneada desde `?servidor=&token=` → `one.servidor` con solo el origin; `fusionarBancoExtra(nuevas, estado, idsLocales)` excluye reportadas e ids locales y purga reportadas; `leerConfiguracion` sanea; polyfill `AbortSignal.timeout`; `console.warn` corto sin token) y `atomo.js` (estado puro ≤ 4 anillos, SVG 320 con etiquetas bajo los nodos, `role=group`). Sondeo cada 5 s con primer sondeo inmediato, `parcial` TERMINAL (contrato real de `servidor/cola.js`), tope 120, pausa en `visibilitychange`/`pagehide persisted` y reanudación en `pageshow`, guardia post-await; la pantalla de espera se repinta al terminar ("Jugar la tanda"). `sw.js` no cachea la URL de navegación con query (el token no entra en Cache Storage). 412 tests, 52 e2e. Revisión final Opus ejecutando la app encontró 4 Critical (parcial terminal, espera muerta, pagehide, token en Cache Storage) + adversarial nemotron gratis 15 hallazgos (11 aceptados, 4 asumidos con motivo en `adversarial-triage.md`). **Carlos: abrir en el iPhone el enlace de conexión** `https://carlostorresadvisory.github.io/one/?servidor=https://one.ctadvisory.es&token=<TOKEN_ONE>` (token: `grep TOKEN_ONE /opt/one/.env` en el VPS; también en el scratchpad de la sesión del 14-sep) y probar: punto verde, chip de nuevas, mantener pulsada Economía → átomo → Generar.
- **Claves de Gemini (14-sep, 19:45-20:00)**: `GEMINI_API_KEY_GRATIS` (AI Studio, proyecto SIN facturación, creada por Carlos; validada HTTP 200; modelos flash/flash-lite/gemma) está en `ONE/.env` local y en `/opt/one/.env` del VPS → es la que entra en la cascada gratis de v0.2b1.1. `GEMINI_API_KEY` (motor comercial de CT Advisory, DE PAGO) está solo en `ONE/.env` local y NO se usa ni se copia mientras `PERMITIR_PAGO=0`. Carlos decidió (21:31) NO regenerarla: se usa tal cual. Regla: ninguna clave se imprime ni va a logs ni a git.
- **Propuesta pendiente de Carlos (19:20): amplitud del átomo** ("finanzas, reestructuración, física cuántica"): nodo "Más…" en todos los anillos (servidor `/subtemas` con `excluir`), 6 anillos, hilos de finanzas/M&A/reestructuración en el anillo 1 de Economía, prompt de subtemas con amplitud + especificidad. Sin respuesta al cierre; si dice sí → spec §4 enmienda + plan corto v0.2b3.
- **Deudas**: e2e flaky de Wikimedia real ("Tarea 3b") → `page.route`; test intermitente de concurrencia en `tests/servidor-cola.test.js`; e2e con esperas de reloj real (usar `page.clock`); tope de 120 sondeos por validar con una tanda real; `RADIO_NUCLEO` 48 con textos largos por ver en el iPhone.
- **Spec §8 "Protagonismo visual" APROBADA por Carlos (17:51)**: visual arriba al 40 % en toda tarjeta revelada (partida y repaso), cascada que nunca la quita (mín. 30 %), tres capas (imagen → visual de datos → tarjeta tipográfica `construirVisualClave` dibujada por la app), pantalla completa con un toque, entrada suave al responder; la tarjeta sin responder no cambia. **Se construye justo después de B2 T1** (mismos ficheros: app.js/estilos.css/visuales.js). Escribir plan corto v0.2a2 y ejecutar con SDD + Opus final.
- **Tanda real de prueba del servidor** `t-mu1em0xi-0` (economía, 5, cascada gratis, 0 $, 17:42→17:54): estado `parcial`, 2 aprobadas de 5 (las dos V/F, nivel 2, confianza 0,96/0,99, correctas y útiles: subida de tipos→inflación; Keynes vs ley de Say). Los 3 huecos NO fueron rechazos del verificador: la GENERACIÓN de test4/ordenar/error falló con todos los modelos gratis ("Ningún modelo respondió": truncados/429). `resolverPregunta` (explicación corta + visual) también falló en gratis: las 2 llegaron con explicación de ~60 palabras y sin visual (la app las cubrirá con la tarjeta tipográfica de §8). 19 llamadas en `llamadas.log`. **Conclusiones**: (1) la cascada gratis no da para producción: hace falta `PERMITIR_PAGO=1` con `TOPE_EUR_DIA` (propuesta 0,30 $/día; una tanda urgente de 10 ≈ 0,01-0,02 $) — decisión de Carlos pendiente; (2) deuda v0.2b1.1: el prompt del generador debe pedir ya la explicación ≤ 40 palabras para no depender de la segunda pasada; registrar en `servidor.log` también los fallos de `resolverPregunta`; (3) los ids `srv-` y el formato servido son válidos para la app.
- **Decisión de Carlos (18:07): SIN PAGO. "Hay que cablear los gratis para que funcionen bien o buscar nuevas APIs gratis si hacen falta"** (no urgente). Entrega v0.2b1.1 "cascada gratis robusta", después del cliente y de §8: (1) lotes más pequeños para test4/ordenar/error (2-3 por llamada) y `maxTokens` suficiente; (2) reintento con espera creciente ante 429 y ante JSON truncado (pedir menos en el reintento); (3) `reasoning: {exclude: true}` en los modelos que razonan (truncan el JSON gastando tokens ocultos, visto ya el 13-sep); (4) ampliar `MODELOS` con más `:free` del catálogo de OpenRouter que devuelvan JSON fiable (auditarlos con `tools/revisar-muestra.js` o un barrido: 5 preguntas por modelo, medir % de JSON válido y latencia); (5) cascadas separadas por fase (generar / verificar / visual) para que el fallo de una no arrastre a las otras; (6) que el generador pida ya la explicación ≤ 40 palabras y el visual en la misma llamada cuando el modelo sea capaz, para no depender de `resolverPregunta`; (7) medir en `servidor.log` la tasa de éxito por modelo y fase y podar los que no rinden. Si con eso no basta, buscar proveedores gratis fuera de OpenRouter (Groq, Cerebras, Google AI Studio, Mistral) con su propia clave en el `.env` del VPS, siempre con la misma regla de verificación por modelo distinto.

**v0.2a.1 PUBLICADA (14-sep, 17:35; sw `one-v11`)** — Repaso infinito con TODO el banco (spec §7): respondidas por prioridad → no respondidas reveladas (borde neutro, `· sin responder`, `listarNoRespondidas` round-robin por área y nivel), sin tarjeta de cierre, vueltas encadenadas con huecos perezosos (2 tarjetas construidas al abrir), ventana de 9 puntos en el mazo. 235 tests, 41 e2e. Opus encontró el Critical de UX (puntos) y el Important de rendimiento (295 tarjetas de golpe).

**v0.2b1 DESPLEGADA (14-sep, 17:40) — servidor de generación (`servidor/`)**, fusionada en main (557e2d9; 356 tests), worktree y ramas borrados. En el VPS: `bash servidor/desplegar.sh` → contenedores `servidor-one-servidor-1` (127.0.0.1:8787) y `servidor-caddy-1` (80/443); `https://one.ctadvisory.es/salud` → 200 ok; sin token → 401. Primera tanda real de prueba lanzada a las 17:42 (`t-mu1em0xi-0`, economía, 5, cascada gratis, 0 $). Ronda final (Opus con servidor real + adversarial ultra): 25 hallazgos corregidos con test (C1 registrarLog, C2 /salud 503, C3 servidas por /trabajo, A1 verificador nunca = generador…), re-review aprobada. Datos históricos de la construcción: plan `docs/superpowers/plans/2026-09-14-one-v0.2b1-servidor.md`, ledger `C:\Users\torre\ONE-wt\v0.2b1\.superpowers\sdd\2026-09-14-one-v0.2b1-servidor\progress.md`. Hechas y revisadas T1 (`tools/prompts-preguntas.js` + `servidor/generacion.js`: generar 4 tipos con MEZCLA → verificar en una llamada por modelo distinto → `validarPregunta` → `resolverPregunta`; `producirTanda` devuelve `{aprobadas, rechazadas, coste, modelos, fallos, pedidas, obtenidas}`) y T2 (`servidor/almacen.js` con `conCerrojo` por fichero y escritura atómica; `servidor/cola.js` con cola única, urgentes que se cuelan entre lotes de 5, objetivo 30 = 60 % áreas flojas + 40 % rutas átomo, purga 30 días / 2.000, tope fondo 32); 268 tests. **T3 en marcha** (agente Sonnet relanzado a las 15:30 tras colgarse el primero): `servidor/index.js` (API §3.3 con token `TOKEN_ONE`, rate limit, CORS, `/salud /estado /generar /trabajo/:id /subtemas /reportar`, colchón nocturno 2-7 h Madrid), `Dockerfile`, `docker-compose.yml` (one-servidor 127.0.0.1:8787 + caddy 80/443), `Caddyfile`, `desplegar.sh`, `DESPLIEGUE.md`, `.gitignore` (`.env`, `datos-servidor/`). Si el agente no acabó: mirar `git -C C:\Users\torre\ONE-wt\v0.2b1 status` y `log`, y relanzar T3 con el brief `task-3-brief.md` + las decisiones del ledger.
- **VPS listo**: `ssh root@31.70.132.252` (llave del portátil; Tailscale del portátil apagado). Repo clonado en `/opt/one` (hay que `git pull` tras fusionar), `/opt/one-datos` creado (uid 1000), `/opt/one/.env` escrito (600) con `OPENROUTER_API_KEY` (copiada por SSH sin mostrarla, autorizado por Carlos), `TOKEN_ONE` (copia local en el scratchpad de la sesión `token-one.txt`; si se pierde, leerlo en el VPS con `grep TOKEN_ONE /opt/one/.env`), `PERMITIR_PAGO=0`, `TOPE_EUR_DIA=0`. DNS `one.ctadvisory.es` → `31.70.132.252` creado por Carlos y resolviendo. **Falta**: revisión de T3 + revisión final Opus de la rama + adversarial gratis → fusionar en main y push → en el VPS: `ufw allow 80/tcp && ufw allow 443/tcp`, `cd /opt/one && git pull && bash servidor/desplegar.sh`, `curl https://one.ctadvisory.es/salud` → prueba real de una tanda de 5 con cascada gratis (0 $) y revisión humana con Carlos → enlace de configuración para el iPhone `https://carlostorresadvisory.github.io/one/?servidor=https://one.ctadvisory.es&token=<TOKEN_ONE>` (lo consume B2).
- **v0.2b2 (siguiente)**: cliente (`sincronizacion.js`, `one.servidor`, `one.bancoExtra`, `POST /estado` al abrir y al acabar partida, pantalla Átomo con mantener pulsado en el área, Generar → espera con "Jugar mientras / Repasar mientras", chip "Tanda lista"), spec §4. Presupuesto de generación: Carlos aún no ha fijado cifra → `PERMITIR_PAGO=0`.
- Deuda de docs del controlador: spec §3.2 (id con contador) y §3.4 (mezcla de 4 tipos, `promptUsuarioVF` en prompts compartidos); README sin sección "Servidor (v0.2)" hasta T3; actualizar `one-estado-v0.md` en memoria.

**v0.1e PUBLICADA (13-sep, 18:25; main = rama `v0.1d-ajustes-movil`, sw `one-v9`)** — "un visual en cada pregunta y explicaciones cortas". Spec `docs/superpowers/specs/2026-09-13-one-v0.1e-visual-en-todas-design.md` (§1: las funciones `generarVisualYExplicacion` / `verificarVisualYExplicacion` de `tools/visualizar.js` son las que usará el servidor de v0.2; §2 esquema del campo `visual`: formula, linea-tiempo, barras, comparacion, flujo, dato, con límites; §3 pipeline; §4 `visuales.js`). Plan `docs/superpowers/plans/2026-09-13-one-v0.1e-visual-en-todas.md`; libro de a bordo con TODOS los rulings e incidentes: `.superpowers/sdd/2026-09-13-one-v0.1e-visual-en-todas/progress.md` (git-ignored); informes de tareas, adversarial (`adversarial-v01e.md` + triage) y ola final en la misma carpeta.
- **Datos**: 167 imágenes (revalidación visual completa, campo `revalidacionVisual`) + 98 visuales = 263/295 (89 %) con algo visual; 32 sin nada (abstractas: economía, lógica, filosofía, tecnología; lista con `node -e` sobre banco e imagenes); explicaciones ≤ 40 palabras en 293/295 (largas originales en `datos/explicaciones-largas.json`); `datos/visuales-excluidos.json` = rechazos humanos (no se regeneran esos tipos). Coste real del 13-sep: 0,3406 $ (0,50 $ aprobados por Carlos para ese día; el permiso NO es permanente: pedir de nuevo).
- **Lo que trae en la interfaz**: visual SVG en la caja de la imagen cuando no hay imagen de Commons (imagen manda; si la imagen falla al cargar, entra el visual); enunciado entero sin responder (cascada propia: 15 px → opciones compactas → clamp calculado, sin toques); cierre "¿Seguro que quieres terminar?"; chevrón oculto mientras la pista textual está visible; tarjeta hasta abajo (zona segura sin triplicar); imagen hasta 55 vh; prompt de Preguntar a pide esquema/tabla/fórmula; service worker con instalación atómica del núcleo.
- **Lecciones del 13-sep (repetir en v0.2)**: (1) la revisión final de rama con Opus volvió a encontrar el único Critical (SVG desbordando su caja sin que `scrollHeight` lo viera) → nunca fusionar sin ella; (2) dos agentes distintos lanzaron dos `--aplicar` a la vez → poner un cerrojo de fichero en el código, no en los briefs; (3) el verificador acepta cifras "típicas" con fuente de buena pinta → barras/dato solo con fuente institución+año y autoverificación, y revisión humana de esos tipos antes de publicar; (4) el tope `--tope-eur` se compara con el gasto ACUMULADO del día en `datos/llamadas.log`; (5) no descartar imágenes por "el modelo no respondió".
- **Pendiente de Carlos**: probar v0.1e en el iPhone (visuales en tarjetas sin imagen, explicaciones cortas, enunciado entero, tarjeta hasta abajo, chevrón sin pisar la pista) y decidir si las 32 sin visual se quedan así.
- **Deuda anotada para v0.2**: filtro de exclusión de `visualizar.js` actúa después de pagar la llamada (mover antes de `resolverPregunta`); cerrojo entre modos `--aplicar`; `--area` ignorado en `--reverificar-visuales`; signo negativo en barras; títulos de barras > 16 caracteres se recortan; `extraerJson` devuelve el primer JSON en silencio si hay dos distintos; foco de teclado ↑/↓; extraer `montarMazo`/`ajustarEncaje` de `app.js`.

**Pendiente**
1. **Feedback de Carlos** desde el iPhone sobre v0.1c (gesto vertical, confianza dentro de la tarjeta, plegado, Preguntar a, imágenes, repaso). Decisión tomada sin preguntar y que Carlos puede revertir: la imagen abierta con "Ver imagen" sustituye la explicación mientras está abierta (alternancia reversible). Nota de la ola de corrección final (13-sep): el autocorrector de posición del mazo (parche defensivo de la Tarea 2, causa raíz no aislada entonces) se ha ELIMINADO — la causa raíz sí se encontró (`.mazo` con `overflow:hidden` se comportaba como contenedor de scroll de verdad) y el fix es CSS (`overflow: clip`) más una red de seguridad de una línea en `render()`; ver `.superpowers/sdd/2026-09-12-one-v0.1c-mazo-vertical/ola-final-report.md`.
2. **Carlos** tiene que revisar `docs/revision-utilidad-2026-09-12.md` (3 preguntas nuevas por área con su nota) y decir si el criterio va bien.
3. **Imágenes**: repetir `node tools/buscar-imagenes.js --revalidar --aplicar` cuando los modelos gratis respondan (≈50 imágenes solo pasaron el filtro de relevancia básico); las flojas también caen con "esta pregunta está mal".
4. **v0.2, lo más importante — generación continua e inteligente** (spec v0.1c §8, decidido con Carlos): dos modos (normal adaptativo por detrás; **átomo**: área en el centro, subtemas orbitando = hilos de `criterio.js`, afinar anillo a anillo sin teclado, botón Generar); servidor mínimo Node en el **VPS de IONOS ya pagado** (con dominio) que guarda la clave; **la verificación nunca se salta, se esconde**: colchón verificado de ~30 preguntas por delante, una sola llamada que devuelve correcta/útil/nivel, y mientras se genera se juega banco o repaso (a elegir). Empezar por brainstorming/spec de v0.2 con Carlos (comprobar acceso SSH al VPS, Node, certificado). Boss estilo esfinge y FSRS después. Deuda técnica: extraer `montarMazo`/`ajustarEncaje` de `app.js` (2.372 líneas, `wc -l`) a un módulo.

**Decisiones fijas (no reabrir)**: sin servidor hasta v0.2 (y entonces el VPS de IONOS, no Cloudflare); GitHub Pages público; paleta oscura + cian, **sin magenta**; "Comenzar", no "Jugar"; confianza Baja/Media/Alta **en lugar de** IDK, dentro de la tarjeta, opcional, editable tras responder; racha 🔥 tal cual, **sin cinta de sellos**; ninguna pantalla ni tarjeta hace scroll y **los tamaños de letra no cambian** para encajar (se pliegan respuestas/imagen); navegación vertical entre preguntas, horizontal solo V/F; vídeo NO (peso, autoplay iOS); imágenes solo de Wikimedia Commons con licencia libre y atribución; verificación de preguntas nunca se salta; V/F 50/50 en el generador; nunca modelos de pago sin `--permitir-pago --tope-eur` aprobado.

**Cómo trabajar con Carlos en este proyecto**: ficha de arranque y línea de contexto en cada mensaje (`~/.claude/CLAUDE.md`); preguntarle todo lo que no esté claro ANTES de construir (lo pidió expresamente); usar superpowers (brainstorming → spec → plan → subagent-driven-development con revisión por tarea) y subagentes Sonnet con la spec como contrato; pasada adversarial con modelo gratis de OpenRouter antes de entregar; commitear por fases y publicar con `git push` (Pages tarda ~1 min); avisarle para que pruebe en el móvil; entregarle la lista de decisiones tomadas por su cuenta.
